FLAGSHIP-PROJECT
A production-style data pipeline that ingests market data via APIs, validates data quality, performs complex transformations, and serves metrics through SQL + dashboards + a lightweight UI.
